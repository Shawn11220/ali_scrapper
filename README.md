Run the requirements file first
Install Python dependencies:

``` md
pip install -r requirements.txt
```
Then install the Playwright browser binaries:
``` md
playwright install
```

Then open the file (aliscrapper.py) and run.
